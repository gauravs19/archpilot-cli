# {{name}} — Service Runbook
<!-- See: rules/19-incident-management.md -->

**Service:** {{name}}
**Owner:** {{author}}
**Last Updated:** {{date}}
**On-Call:** [Team/Contact]

## 1. Service Overview
[One paragraph: what the service does, its criticality, and its dependencies]

| Attribute | Value |
|-----------|-------|
| Language / Runtime | [e.g., Node.js 20] |
| Deployment | [ECS / EKS / Lambda] |
| SLA | 99.9% uptime |
| RTO | < 15 minutes |
| RPO | < 5 minutes |

## 2. Key Dependencies
| Dependency | Type | Fallback |
|-----------|------|---------|
| [ServiceName] | Internal API | Circuit breaker ? cached response |
| [Database] | PostgreSQL | Read replica |

## 3. Health Checks
| Endpoint | Expected Response | Alert If |
|---------|-----------------|---------|
| `GET /health` | `200 {"status":"ok"}` | Non-200 for > 30s |
| `GET /metrics` | Prometheus format | Absent for > 60s |

## 4. Common Alerts & Runbook Steps

### Alert: High Error Rate (> 1%)
1. Check logs: `[log query]`
2. Check dependency health: `[command]`
3. If dependency down ? circuit breaker should activate
4. If not ? escalate to [Team]

### Alert: High Latency (p95 > 1s)
1. Check DB slow query log
2. Check cache hit rate
3. Check upstream dependency latency
4. Scale out if CPU > 70%

### Alert: Pod/Instance Down
1. Check pod status: `kubectl get pods -n [namespace]`
2. Check recent deployments: `[command]`
3. Roll back if recent deploy: `[command]`

## 5. Deployment & Rollback
```bash
# Deploy
[deploy command]

# Rollback
[rollback command]
```

## 6. Escalation Path
| Severity | First Responder | Escalate To | Timeout |
|---------|----------------|-------------|---------|
| P1 | On-call engineer | Eng Manager | 15 min |
| P2 | On-call engineer | Team Lead | 30 min |

---
*Generated by Archpilot CLI — https://gauravs19.github.io/archpilot/*
